# Copy this file to .env (already gitignored) and fill in your own server.
#
# Base URL of the PlanRoute POI server (gas stations / camping / hotels
# along a route — see repository/PoiRepository.kt for the request/response
# contract it must implement). Include the scheme and a trailing slash.
SERVER_BASE_URL=http://192.168.1.100:8080/
