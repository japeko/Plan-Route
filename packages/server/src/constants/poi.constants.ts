// Shared projection field list. Centralized so every query site stays in
// sync when new POI fields are added later (photos, reviews, etc.).
export const POI_LIST_PROJECTION = "name type location address hasGasoline hasElectricCharging hasRestaurant";
